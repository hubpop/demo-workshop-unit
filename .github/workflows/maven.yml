# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Test with Maven
      run: mvn -B test --file pom.xml
      
    - name: Before script
      run: chmod +x mvnw
    - name: SonarCloud Scan
      run: ./mvnw -B verify sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=980de76631fc35d67417fb270b7d67a5cca61266
      env:
        GITHUB_TOKEN: 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
        SONAR_TOKEN: 980de76631fc35d67417fb270b7d67a5cca61266
